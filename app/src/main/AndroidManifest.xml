<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:icon="@drawable/ic_agentboard"
        android:label="@string/app_name"
        android:theme="@style/KeyboardTheme">

        <activity
            android:name="com.zellij.keyboard.MicrophonePermissionActivity"
            android:exported="false"
            android:theme="@style/MicrophonePermissionTheme" />

        <service
            android:name="com.zellij.keyboard.ZellijKeyboardService"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>
    </application>
</manifest>
